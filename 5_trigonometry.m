% "sin(number)" -> compute the value of the sin number, using radians
% units (allows matrix and specify the position of the result).

% "sind(number)" -> compute the value of the sin number, using degrades
% units (allows matrix and specify the position of the result).

% "asin(number)" -> compute the value of the arcosin number, using radians
% unit (allows matrix and specify the position of the result).

% "asind(number)" -> compute the value of the arcosin number, using degrades
% unit (allows matrix and specify the position of the result).

% All the same above with "cos()", "tan()", "sec()", "csc()".

% Convertion radians to degrades = "rad2deg(number)".

% Convertion degrades to radians = "deg2rad(number)".

% Up in the "Search Documentation" can be searched de trygonometry
% functions with all of their info.