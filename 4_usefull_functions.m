% gcd(number_a, number_b) -> compute the M.C.D of the two numbers (allows matrix).

% lcm(number_a, number_b) -> compute the m.c.m of the two numbers (allows matrix).

% isprime(number) -> compute if the number is a prime number. true or
% false (allows matrix).

% primes(number) -> compute all primes since 0 to the number that are
% actually primes.

% prod(set) -> compute the product of all the numbers  (allows matrix).

% perms(set) -> compute all the posible permutations in a ser of numbers.
